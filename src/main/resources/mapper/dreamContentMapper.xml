<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yiweilai.DreamArchive.mapper.DreamContentMapper">

    <insert id="insertDreamContent" parameterType="com.yiweilai.DreamArchive.DTO.DreamContent">
        INSERT INTO dream_content (id, user_id, title, content, emotion, place, time, interpretation, analysis_status, analysis_error, image_url, created_at)
        VALUES (#{id}, #{userId}, #{title}, #{content}, #{emotion}, #{place}, #{time}, #{interpretation}, #{analysisStatus}, #{analysisError}, #{imageUrl}, NOW())
    </insert>

    <select id="selectById" resultType="com.yiweilai.DreamArchive.DTO.DreamContent">
        SELECT id, user_id as userId, title, content, emotion, place, time, interpretation, analysis_status as analysisStatus, analysis_error as analysisError, image_url as imageUrl, created_at as createdAt
        FROM dream_content
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultType="com.yiweilai.DreamArchive.DTO.DreamContent">
        SELECT id, user_id as userId, title, content, emotion, place, time, interpretation, analysis_status as analysisStatus, analysis_error as analysisError, image_url as imageUrl, created_at as createdAt
        FROM dream_content
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <update id="updateTitle">
        UPDATE dream_content
        SET title = #{title}
        WHERE id = #{id}
    </update>

    <update id="updateInterpretation">
        UPDATE dream_content
        SET interpretation = #{interpretation}
        WHERE id = #{id}
    </update>

    <update id="updateAnalysisStatus">
        UPDATE dream_content
        SET analysis_status = #{status}, analysis_error = #{error}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM dream_content
        WHERE id = #{id}
    </delete>

</mapper>
